# Install

on Linux:

`
git clone https://github.com/scanlime/fadecandy \
  && cd fadecandy/server && make submodules && make
`

then copy make sure to run fadecandy with config:

`./fcserver config.json`

on OS X:

`wget https://github.com/scanlime/fadecandy/raw/master/bin/fcserver-osx`

